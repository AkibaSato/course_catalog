<%= simple_form_for(@subject_courses_relation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :subject_id %>
    <%= f.input :course_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
